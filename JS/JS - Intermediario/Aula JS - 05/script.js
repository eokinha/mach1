let listaAlunos = ['Danilo', 'Elis', 'Fernando']

function exibeListaAlunos(listaDeAlunos) {
    for (let i = 0; i < listaDeAlunos.length; i++) {
        console.log(listaAlunos[i])
    }
}

{
    function soma(z, y, x) {
        return x + y + z;
    }
}